<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hellogroup.teamenu.infrastructure.persistence.mapper.MealPlanMapper">
    
    <select id="selectByDateRange" resultType="com.hellogroup.teamenu.infrastructure.persistence.entity.MealPlanEntity">
        SELECT * FROM meal_plan
        WHERE deleted = 0
        AND plan_date BETWEEN #{startDate} AND #{endDate}
        <if test="weekType != null and weekType != ''">
            AND week_identifier = #{weekType}
        </if>
        ORDER BY plan_date ASC, create_time ASC
    </select>
    
    <delete id="deleteBeforeDate">
        UPDATE meal_plan SET deleted = 1
        WHERE deleted = 0 AND plan_date &lt; #{beforeDate}
    </delete>
    
</mapper>
